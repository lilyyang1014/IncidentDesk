# No environment variables are required for the unchanged official starter.
# Never put secrets in VITE_* variables: they are exposed to the browser.
# DeepSpace manages local platform credentials in the ignored .dev.vars file.
